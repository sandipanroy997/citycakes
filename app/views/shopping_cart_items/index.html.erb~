<div id="shopping_cart_wrapper">
<div id="shopping_cart">
 <h3> Shopping Cart </h3>





<table>
  
  
  
  <thead>
    <tr>
      <th>Product</th>
      <th class="text-right">Weight (pound)</th>
      <th class="text-center">Quantity</th>
      <th class="text-right">Price</th>
      <th class="text-right">Total Price</th>
    </tr>
  </thead>

  <tbody>
    <% @order_item_products.each do |item| %>
    <tr>
      <td>
        
          <%= image_tag item.avatar.url(:thumb) %>
          <span><%= item.name %></span>
        
      </td>
      <td class="text-right"><%= item.weight %></td>
      <td class="text-center">
        <%= select_tag "quantity", options_for_select(Array.new(10).fill{|a| a+1}) %><br/><br/>
        <%= link_to 'remove item', remove_item_orders_path(:id => item), :id => "product_#{item.id}", "data-method" => "delete", "data-remote" => true %>
      </td>
      <td class="text-right"><%= item.price %></td>
      

      <td class="text-right"><%= item.price %></td>
    </tr>
    <% end %>
  </tbody>
     <tfoot>
   <tr>
    <td>Sub Total</td>
    <td colspan=4 class="text-right"></td>
   </tr>
</table>



</div>

<%= link_to 'Order' , confirm_orders_path,:class => "cart-btn" %>
<%= link_to 'Continue Shopping',root_path, :class => "cart-btn" ,:style => "float:left;" %>


</div>
